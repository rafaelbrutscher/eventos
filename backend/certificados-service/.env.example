# ===========================================
# 🎓 CERTIFICADOS SERVICE - DJANGO SETTINGS  
# ===========================================

# Django Core
DEBUG=False
SECRET_KEY=sua-chave-secreta-django-muito-longa-e-segura-aqui

# Database MySQL (compartilhado com outros microserviços)
DB_CONNECTION=mysql
DB_HOST=177.44.248.89
DB_PORT=3306
DB_DATABASE=eventos
DB_USERNAME=eventos
DB_PASSWORD=eventos123

# Redis para Cache e Celery
REDIS_URL=redis://177.44.248.89:6379/0

# Hosts Permitidos (adicione IP da VM)
ALLOWED_HOSTS=localhost,127.0.0.1,177.44.248.89

# Email Configuration (configure com seu provedor)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=seuemail@gmail.com
EMAIL_HOST_PASSWORD=suasenhadeemail

# Email
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=brutscherrafael@gmail.com
MAIL_PASSWORD=csbz qbwx vvhz pyyr
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="eventos@seudominio.com"
MAIL_FROM_NAME="${APP_NAME}"

# Celery Configuration
CELERY_BROKER_URL=redis://177.44.248.89:6379/0
CELERY_RESULT_BACKEND=redis://177.44.248.89:6379/0

# ===========================================
# 📝 INSTRUÇÕES:
# ===========================================
# 1. cp .env.example .env
# 2. Gere uma SECRET_KEY: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# 3. Configure EMAIL_HOST_USER e EMAIL_HOST_PASSWORD
# 4. Adicione o IP da VM em ALLOWED_HOSTS